<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScrollRackCompanion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/olive</item>
        <item name="android:editTextColor">@color/olive</item>
    </style>

    <style name="Theme.ScrollRackCompanion" parent="Base.Theme.ScrollRackCompanion" />
</resources>